import { connect } from "react-redux";
import CardReplacement from "../../../views/Support/CardReplacement";

const mapStateToProps = (state) => {
  return {

  }
}

const mapDispatchToProps = (dispatch) => {
  const dispatchProps = {

  }

  return dispatchProps
}

const CardFile = connect(mapStateToProps, mapDispatchToProps)(CardReplacement);

export default CardFile;