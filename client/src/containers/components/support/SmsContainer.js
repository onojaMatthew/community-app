import { connect } from "react-redux";
import Sms from "../../../views/Support/Sms";

const mapStateToProps = (state) => {
  return {

  }
}

const mapDispatchToProps = (dispatch) => {
  const dispatchProps = {

  }

  return dispatchProps
}

const SmsFile = connect(mapStateToProps, mapDispatchToProps)(Sms);

export default SmsFile;