import { connect } from "react-redux";
import CardUpdateContent from "../../../views/Support/CardUpdateContent";

const mapStateToProps = (state) => {
  return {

  }
}

const mapDispatchToProps = (dispatch) => {
  const dispatchProps = {

  }

  return dispatchProps;
}

const CardUpdate = connect(mapStateToProps, mapDispatchToProps)(CardUpdateContent);

export default CardUpdate;