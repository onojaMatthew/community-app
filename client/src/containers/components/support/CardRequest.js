import { connect } from "react-redux";
import CardRequest from "../../../views/Support/CardRequest";

const mapStateToProps = (state) => {
  return {

  }
}

const mapDispatchToProps = (dispatch) => {
  const dispatchProps = {

  }

  return dispatchProps;
}

const CardRequestFile = connect(mapStateToProps, mapDispatchToProps)(CardRequest);

export default CardRequestFile;